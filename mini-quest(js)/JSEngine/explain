자바스크립트 코드가 웹페이지에서 어떻게 실행되는지?
-> 
자바스크립트 코드는 자바스크립트 엔진에 의해 파싱된다. 그 뒤에 바이트코드가 실행된다.
그 뒤, 함수와 변수가 콜 스택에 쌓이는데 이 때 호이스팅으로 인해 선언부는 해당 스코프의 최상단에서 선언된다.
함수가 차례대로 실행되면서 비동기 콜백들은 웹 API에 의해 처리되고 이벤트큐에 들어간다.
이벤트루프는 계속해서 콜스택을 보고 비었으면 그 때 이벤트 큐에서 하나씩 꺼내어 콜스택에 올린다.

자바스크립트 엔진은 여기에서 자바스크립트 코드를 파싱해주고, AST로 변환한다.
가비지 컬렉터를 통해 필요없는 것은 없애준다. 그리고 V8같은 경우에는 자주 사용되는 코드를 핫스팟으로도 지정해준다.